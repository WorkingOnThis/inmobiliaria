# Owner Current Account MVP — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Completar la cuenta corriente del propietario con filtros, dos KPI cards, y el EntryDetailDialog conectado al menú `···`.

**Architecture:** 4 cambios quirúrgicos en archivos existentes. Sin archivos nuevos, sin schema changes. La tabla del ledger ya soporta `isOwnerView`; solo faltaba conectar los handlers y ocultar las acciones destructivas.

**Tech Stack:** Next.js App Router · React · TanStack Query · Drizzle ORM · shadcn/ui · Tailwind v4

---

## Mapa de archivos

| Archivo | Tipo | Qué cambia |
|---|---|---|
| `src/components/ledger/entry-detail-dialog.tsx` | Modify | Agregar prop `readOnly?: boolean` |
| `src/components/tenants/ledger-table.tsx` | Modify | Ocultar cancel/anular cuando `isOwnerView = true` |
| `src/app/api/owners/[id]/cuenta-corriente/route.ts` | Modify | Agregar KPI `totalPendiente` |
| `src/components/owners/owner-tab-current-account.tsx` | Modify | Filtros, KPIs, EntryDetailDialog |

---

## Task 1: Prop `readOnly` en EntryDetailDialog

**Files:**
- Modify: `src/components/ledger/entry-detail-dialog.tsx`

- [ ] **Paso 1: Agregar prop `readOnly` al tipo Props y usarlo en `isEditable`**

Abrir `src/components/ledger/entry-detail-dialog.tsx`. Localizar el tipo `Props` (línea ~28) y la definición de `isEditable` (línea ~71).

Cambiar el tipo Props de:
```ts
type Props = {
  entry: LedgerEntry | null;
  onOpenChange: (open: boolean) => void;
  onSave: (data: EntryEditData) => Promise<void>;
  isSplitContract?: boolean;
  onBeneficiarioOverride?: (entryId: string, value: string | null) => void;
};
```
a:
```ts
type Props = {
  entry: LedgerEntry | null;
  onOpenChange: (open: boolean) => void;
  onSave: (data: EntryEditData) => Promise<void>;
  isSplitContract?: boolean;
  onBeneficiarioOverride?: (entryId: string, value: string | null) => void;
  readOnly?: boolean;
};
```

Cambiar la firma de la función de:
```ts
export function EntryDetailDialog({ entry, onOpenChange, onSave, isSplitContract = false, onBeneficiarioOverride }: Props) {
```
a:
```ts
export function EntryDetailDialog({ entry, onOpenChange, onSave, isSplitContract = false, onBeneficiarioOverride, readOnly = false }: Props) {
```

Cambiar la línea de `isEditable` de:
```ts
const isEditable = entry !== null && !entry.isAutoGenerated && entry.estado !== "conciliado" && entry.estado !== "cancelado";
```
a:
```ts
const isEditable = !readOnly && entry !== null && !entry.isAutoGenerated && entry.estado !== "conciliado" && entry.estado !== "cancelado";
```

- [ ] **Paso 2: Verificar que el dialog de inquilinos sigue funcionando**

Abrir la ficha de un inquilino en el browser → tab "Cuenta corriente" → hacer click en el menú `···` de cualquier entrada → "Ver detalle". Verificar que el dialog abre y los campos son editables (o read-only para entradas auto-generadas) igual que antes.

- [ ] **Paso 3: Commit**

```bash
git add src/components/ledger/entry-detail-dialog.tsx
git commit -m "feat(entry-detail-dialog): add readOnly prop to force view-only mode"
```

---

## Task 2: Ocultar acciones destructivas en LedgerTable cuando isOwnerView

**Files:**
- Modify: `src/components/tenants/ledger-table.tsx`

- [ ] **Paso 1: Envolver las opciones de cancelar y anular con condición `!isOwnerView`**

Abrir `src/components/tenants/ledger-table.tsx`. Localizar el bloque del `DropdownMenuContent` (alrededor de la línea 465). El bloque actual es:

```tsx
<DropdownMenuContent align="end">
  <DropdownMenuItem onClick={() => onViewDetail(entry)}>
    Ver detalle
  </DropdownMenuItem>
  {isPunitorio && entry.estado !== "conciliado" && (
    <DropdownMenuItem
      className="text-destructive focus:text-destructive"
      onClick={() => onCancelEntry(entry)}
    >
      Cancelar punitorio
    </DropdownMenuItem>
  )}
  {!isPunitorio && isCancelable(entry) && (
    <DropdownMenuItem
      className="text-destructive focus:text-destructive"
      onClick={() => onCancelEntry(entry)}
    >
      Cancelar movimiento
    </DropdownMenuItem>
  )}
  {entry.reciboNumero && ["conciliado", "pago_parcial"].includes(entry.estado) && (
    <DropdownMenuItem
      className="text-destructive focus:text-destructive"
      onClick={() => onAnularRecibo(entry.reciboNumero!)}
    >
      Anular recibo
    </DropdownMenuItem>
  )}
</DropdownMenuContent>
```

Reemplazarlo con:

```tsx
<DropdownMenuContent align="end">
  <DropdownMenuItem onClick={() => onViewDetail(entry)}>
    Ver detalle
  </DropdownMenuItem>
  {!isOwnerView && isPunitorio && entry.estado !== "conciliado" && (
    <DropdownMenuItem
      className="text-destructive focus:text-destructive"
      onClick={() => onCancelEntry(entry)}
    >
      Cancelar punitorio
    </DropdownMenuItem>
  )}
  {!isOwnerView && !isPunitorio && isCancelable(entry) && (
    <DropdownMenuItem
      className="text-destructive focus:text-destructive"
      onClick={() => onCancelEntry(entry)}
    >
      Cancelar movimiento
    </DropdownMenuItem>
  )}
  {!isOwnerView && entry.reciboNumero && ["conciliado", "pago_parcial"].includes(entry.estado) && (
    <DropdownMenuItem
      className="text-destructive focus:text-destructive"
      onClick={() => onAnularRecibo(entry.reciboNumero!)}
    >
      Anular recibo
    </DropdownMenuItem>
  )}
</DropdownMenuContent>
```

- [ ] **Paso 2: Verificar que la vista de inquilino no cambió**

Abrir la ficha de un inquilino → Cuenta corriente → menú `···` de una entrada con recibo. Verificar que sigue mostrando "Anular recibo" y "Cancelar movimiento".

- [ ] **Paso 3: Commit**

```bash
git add src/components/tenants/ledger-table.tsx
git commit -m "feat(ledger-table): hide destructive actions when isOwnerView"
```

---

## Task 3: Agregar KPI `totalPendiente` en la API

**Files:**
- Modify: `src/app/api/owners/[id]/cuenta-corriente/route.ts`

- [ ] **Paso 1: Agregar import de `inArray` y la query de pendientes**

Abrir `src/app/api/owners/[id]/cuenta-corriente/route.ts`.

Cambiar la línea de imports de drizzle-orm de:
```ts
import { and, eq, sql, sum } from "drizzle-orm";
```
a:
```ts
import { and, eq, inArray, sql, sum } from "drizzle-orm";
```

Reemplazar el bloque `Promise.all` completo de:
```ts
const [entries, ytdResult] = await Promise.all([
  db
    .select()
    .from(tenantLedger)
    .where(
      and(
        eq(tenantLedger.propietarioId, propietarioId),
        eq(tenantLedger.impactaPropietario, true)
      )
    )
    .orderBy(tenantLedger.period, tenantLedger.tipo),

  db
    .select({ total: sum(tenantLedger.monto) })
    .from(tenantLedger)
    .where(
      and(
        eq(tenantLedger.propietarioId, propietarioId),
        eq(tenantLedger.estado, "conciliado"),
        eq(tenantLedger.impactaPropietario, true),
        sql`extract(year from ${tenantLedger.dueDate}::date) = ${currentYear}`
      )
    )
    .then((rows) => rows[0]),
]);
```
por:
```ts
const [entries, ytdResult, pendienteResult] = await Promise.all([
  db
    .select()
    .from(tenantLedger)
    .where(
      and(
        eq(tenantLedger.propietarioId, propietarioId),
        eq(tenantLedger.impactaPropietario, true)
      )
    )
    .orderBy(tenantLedger.period, tenantLedger.tipo),

  db
    .select({ total: sum(tenantLedger.monto) })
    .from(tenantLedger)
    .where(
      and(
        eq(tenantLedger.propietarioId, propietarioId),
        eq(tenantLedger.estado, "conciliado"),
        eq(tenantLedger.impactaPropietario, true),
        sql`extract(year from ${tenantLedger.dueDate}::date) = ${currentYear}`
      )
    )
    .then((rows) => rows[0]),

  db
    .select({ total: sum(tenantLedger.monto) })
    .from(tenantLedger)
    .where(
      and(
        eq(tenantLedger.propietarioId, propietarioId),
        eq(tenantLedger.impactaPropietario, true),
        inArray(tenantLedger.estado, ["pendiente", "registrado"])
      )
    )
    .then((rows) => rows[0]),
]);
```

Reemplazar el `return NextResponse.json` de:
```ts
return NextResponse.json({
  kpis: { totalLiquidadoYTD: Number(ytdResult?.total ?? 0) },
  ledgerEntries: entries,
});
```
por:
```ts
return NextResponse.json({
  kpis: {
    totalLiquidadoYTD: Number(ytdResult?.total ?? 0),
    totalPendiente: Number(pendienteResult?.total ?? 0),
  },
  ledgerEntries: entries,
});
```

- [ ] **Paso 2: Verificar la respuesta de la API**

En el browser, abrir la ficha de un propietario que tenga entradas en su cuenta corriente. En DevTools → Network, buscar la llamada a `/api/owners/.../cuenta-corriente`. Verificar que la respuesta incluye:
```json
{
  "kpis": {
    "totalLiquidadoYTD": <número>,
    "totalPendiente": <número>
  },
  "ledgerEntries": [...]
}
```

- [ ] **Paso 3: Commit**

```bash
git add src/app/api/owners/[id]/cuenta-corriente/route.ts
git commit -m "feat(api): add totalPendiente KPI to owner cuenta-corriente"
```

---

## Task 4: Actualizar OwnerTabCurrentAccount

**Files:**
- Modify: `src/components/owners/owner-tab-current-account.tsx`

- [ ] **Paso 1: Reemplazar el contenido completo del archivo**

Reemplazar todo el contenido de `src/components/owners/owner-tab-current-account.tsx` con:

```tsx
"use client";

import { useState } from "react";
import { useQuery } from "@tanstack/react-query";
import { Card, CardContent } from "@/components/ui/card";
import { ToggleGroup, ToggleGroupItem } from "@/components/ui/toggle-group";
import { LedgerTable, type LedgerEntry } from "@/components/tenants/ledger-table";
import { EntryDetailDialog } from "@/components/ledger/entry-detail-dialog";

type Props = {
  propietarioId: string;
};

type CuentaCorrienteData = {
  kpis: { totalLiquidadoYTD: number; totalPendiente: number };
  ledgerEntries: LedgerEntry[];
};

export function OwnerTabCurrentAccount({ propietarioId }: Props) {
  const [activeFilters, setActiveFilters] = useState<Set<string>>(new Set(["pending", "paid"]));
  const [selectedDetailEntry, setSelectedDetailEntry] = useState<LedgerEntry | null>(null);

  const { data, isLoading, isError } = useQuery<CuentaCorrienteData>({
    queryKey: ["owner-cuenta-corriente", propietarioId],
    queryFn: () =>
      fetch(`/api/owners/${propietarioId}/cuenta-corriente`).then((r) => r.json()),
  });

  if (isLoading) {
    return <div className="p-4 text-sm text-muted-foreground">Cargando cuenta corriente...</div>;
  }

  if (isError || !data) {
    return (
      <div className="p-4 text-sm text-destructive">
        Error al cargar la cuenta corriente.
      </div>
    );
  }

  const { kpis, ledgerEntries } = data;

  // In the owner view, "pending" includes overdue entries (entries past due date
  // are overdue because the tenant hasn't paid, not because the owner hasn't paid)
  const effectiveFilters = new Set(activeFilters);
  if (activeFilters.has("pending")) effectiveFilters.add("overdue");

  return (
    <div className="flex flex-col gap-4 pb-8">
      {/* KPI Cards */}
      <div className="grid grid-cols-2 gap-3 px-4 pt-4">
        <Card className="rounded-[var(--radius-lg)] border py-0 gap-0">
          <CardContent className="p-4">
            <p className="text-xs font-medium text-muted-foreground uppercase tracking-wide">
              Liquidado {new Date().getFullYear()}
            </p>
            <p className="text-2xl font-bold mt-1 font-mono text-income">
              ${kpis.totalLiquidadoYTD.toLocaleString("es-AR")}
            </p>
          </CardContent>
        </Card>
        <Card className="rounded-[var(--radius-lg)] border py-0 gap-0">
          <CardContent className="p-4">
            <p className="text-xs font-medium text-muted-foreground uppercase tracking-wide">
              Pendiente de liquidar
            </p>
            {kpis.totalPendiente > 0 ? (
              <p className="text-2xl font-bold mt-1 font-mono text-warning">
                ${kpis.totalPendiente.toLocaleString("es-AR")}
              </p>
            ) : (
              <p className="text-2xl font-bold mt-1 text-income">Al día</p>
            )}
          </CardContent>
        </Card>
      </div>

      {/* Filters */}
      <div className="px-4">
        <ToggleGroup
          type="multiple"
          value={[...activeFilters]}
          onValueChange={(v) => setActiveFilters(new Set(v))}
        >
          <ToggleGroupItem value="pending" className="text-xs h-8 px-3">Pendientes</ToggleGroupItem>
          <ToggleGroupItem value="paid" className="text-xs h-8 px-3">Pagados</ToggleGroupItem>
          <ToggleGroupItem value="future" className="text-xs h-8 px-3">Futuros</ToggleGroupItem>
        </ToggleGroup>
      </div>

      {/* Ledger table */}
      {ledgerEntries.length === 0 ? (
        <div className="px-4 py-8 text-center text-sm text-muted-foreground">
          No hay entradas en la cuenta corriente todavía.
        </div>
      ) : (
        <LedgerTable
          entries={ledgerEntries}
          montoOverrides={{}}
          selectedIds={new Set()}
          onToggleSelect={() => {}}
          onSelectMonth={() => {}}
          onDeselectMonth={() => {}}
          onMontoChange={() => {}}
          onAnularRecibo={() => {}}
          onCancelEntry={() => {}}
          onViewDetail={setSelectedDetailEntry}
          activeFilters={effectiveFilters}
          isOwnerView={true}
        />
      )}

      <EntryDetailDialog
        entry={selectedDetailEntry}
        onOpenChange={(open) => { if (!open) setSelectedDetailEntry(null); }}
        readOnly={true}
        onSave={() => Promise.resolve()}
      />
    </div>
  );
}
```

- [ ] **Paso 2: Verificar en el browser**

Abrir la ficha de un propietario que tenga entradas → tab "Cuenta corriente":

1. ✓ Se ven dos KPI cards: "Liquidado 2026" y "Pendiente de liquidar"
2. ✓ Se ven los tres filtros: Pendientes / Pagados / Futuros (activos por defecto Pendientes + Pagados)
3. ✓ Hacer click en el menú `···` de cualquier entrada → solo aparece "Ver detalle"
4. ✓ Hacer click en "Ver detalle" → se abre el dialog con los datos de la entrada, en modo lectura (campos deshabilitados, sin botón Guardar)
5. ✓ Hacer click en una fila directamente → también abre el dialog
6. ✓ Cerrar el dialog → vuelve a null, no queda estado colgado
7. ✓ Cambiar filtros → la tabla se actualiza mostrando solo las entradas del filtro seleccionado

- [ ] **Paso 3: Verificar que la vista de inquilinos no cambió**

Abrir la ficha de un inquilino → Cuenta corriente → confirmar que todo sigue igual (CobroPanel, filtros, emit, cancel, anular).

- [ ] **Paso 4: Commit**

```bash
git add src/components/owners/owner-tab-current-account.tsx
git commit -m "feat(owner-cc): add filters, dual KPI cards, and EntryDetailDialog"
```

---

## Task 5: Mover ítem a HISTORIAL y actualizar PENDIENTES

**Files:**
- Modify: `PENDIENTES.md`
- Modify: `HISTORIAL.md`

- [ ] **Paso 1: Tachar el ítem en PENDIENTES.md**

En `PENDIENTES.md`, cambiar la línea:
```
- [ ] **Cuenta corriente de propietarios** — dejarla similar a la de inquilinos...
```
por:
```
- [x] **Cuenta corriente de propietarios** — dejarla similar a la de inquilinos...
```

- [ ] **Paso 2: Agregar entrada en HISTORIAL.md**

Agregar al inicio de la lista de completados en `HISTORIAL.md`:
```
- **2026-05-06** — Cuenta corriente de propietarios: filtros (Pendientes/Pagados/Futuros), dos KPI cards (liquidado YTD + pendiente), EntryDetailDialog conectado al menú `···` en modo lectura. Acciones destructivas (cancelar, anular) ocultas en vista propietario.
```

- [ ] **Paso 3: Commit final**

```bash
git add PENDIENTES.md HISTORIAL.md
git commit -m "chore: mark owner current account MVP as complete"
```
