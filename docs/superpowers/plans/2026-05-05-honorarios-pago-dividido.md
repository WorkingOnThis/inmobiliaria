# Modalidad de Pago Dividido — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Agregar la modalidad "split" al contrato: cada ítem del ledger lleva su destinatario (`beneficiario`), la cuenta corriente del inquilino muestra el desglose por destinatario, y la cuenta del propietario distingue visualmente los cobros directos.

**Architecture:** Nueva columna `beneficiario` en `tenant_ledger` (asignada automáticamente por tipo o manualmente). La API `cuenta-corriente` devuelve metadatos de la modalidad split (CBU del propietario + datos bancarios de la agencia). El `CobroPanel` reemplaza el resumen actual con dos filas de destinatarios cuando el contrato es split.

**Tech Stack:** Next.js App Router · Drizzle ORM · PostgreSQL · TanStack Query · shadcn/ui · TypeScript

---

## Mapa de archivos

| Archivo | Acción | Qué cambia |
|---------|--------|-----------|
| `src/db/schema/tenant-ledger.ts` | Modificar | Agregar `beneficiario` y `splitBreakdown` |
| `src/components/contracts/contract-form.tsx` | Modificar | Agregar opción "split" al select de modalidad |
| `src/app/api/tenants/[id]/cuenta-corriente/route.ts` | Modificar | Devolver `splitMeta`, asignar `beneficiario` a punitorios auto-generados |
| `src/app/api/tenants/[id]/ledger/route.ts` | Modificar | Aceptar y auto-asignar `beneficiario` en POST |
| `src/app/api/tenants/[id]/ledger/[entryId]/conciliar/route.ts` | Modificar | Aceptar y guardar `splitBreakdown` en body |
| `src/components/tenants/ledger-table.tsx` | Modificar | Agregar `beneficiario`/`splitBreakdown` al tipo `LedgerEntry`; columna Destino |
| `src/components/tenants/cobro-panel.tsx` | Modificar | Split-aware: mostrar dos destinatarios con CBU |
| `src/components/tenants/tenant-tab-current-account.tsx` | Modificar | Pasar `splitMeta`, manejar `beneficiarioOverrides`, enviar `splitBreakdown` al conciliar |
| `src/components/ledger/add-manual-charge-dialog.tsx` | Modificar | Agregar selector `beneficiario` cuando el contrato es split |
| `src/components/ledger/entry-detail-dialog.tsx` | Modificar | Agregar override efímero de `beneficiario` con advertencia |
| `src/components/owners/owner-tab-current-account.tsx` | Modificar | Pasar `isOwnerView` a `LedgerTable` |

---

## Task 1: Schema — agregar `beneficiario` y `splitBreakdown` a `tenant_ledger`

**Files:**
- Modify: `src/db/schema/tenant-ledger.ts`

- [ ] **Step 1: Agregar las dos columnas**

En `src/db/schema/tenant-ledger.ts`, dentro de `pgTable("tenant_ledger", { ... })`, agregar después de `isAutoGenerated`:

```ts
// Split-payment modality
// "propietario" | "administracion" | "split" | null (null = no es contrato split)
beneficiario: text("beneficiario"),
// JSON guardado al conciliar: { propietario: number, administracion: number }
splitBreakdown: text("splitBreakdown"),
```

- [ ] **Step 2: Aplicar al schema de DB**

```bash
bun run db:push
```

Verificar: `bun run db:studio` → tabla `tenant_ledger` tiene columnas `beneficiario` y `splitBreakdown`.

- [ ] **Step 3: Commit**

```bash
git add src/db/schema/tenant-ledger.ts
git commit -m "feat(schema): add beneficiario and splitBreakdown to tenant_ledger"
```

---

## Task 2: Contract form — opción "split" en modalidad de pago

**Files:**
- Modify: `src/components/contracts/contract-form.tsx`

- [ ] **Step 1: Agregar SelectItem**

En `contract-form.tsx`, buscar el bloque del Select de "Modalidad de pago" (alrededor de línea 922). Agregar después de `<SelectItem value="B">`:

```tsx
<SelectItem value="split">Modalidad Split — el inquilino paga dividido</SelectItem>
```

- [ ] **Step 2: Actualizar el helper text**

En el mismo bloque, el `<p>` de descripción debajo del Select actualmente maneja solo "A" y "B". Agregar el caso "split":

```tsx
<p className="text-xs text-muted-foreground">
  {step2.paymentModality === "A"
    ? "La inmobiliaria cobra por su CBU y liquida al propietario. Se aplica un recargo del 1% sobre el alquiler, descontado de la liquidación."
    : step2.paymentModality === "B"
    ? "El inquilino paga directo al CBU del propietario. La inmobiliaria no interviene en el cobro. Sin recargo de administración."
    : "El inquilino recibe instrucciones de transferir una parte al propietario y otra a la administración. La división se calcula según el % de administración configurado."}
</p>
```

- [ ] **Step 3: Verificar en UI**

Correr `bun dev`, ir a `/contratos/nuevo` → Paso 2 → select "Modalidad de pago" → verificar que aparece "Modalidad Split".

- [ ] **Step 4: Commit**

```bash
git add src/components/contracts/contract-form.tsx
git commit -m "feat(contracts): add split payment modality option to contract form"
```

---

## Task 3: API cuenta-corriente — devolver `splitMeta` + asignar `beneficiario` en punitorios

**Files:**
- Modify: `src/app/api/tenants/[id]/cuenta-corriente/route.ts`

- [ ] **Step 1: Agregar imports necesarios**

Al inicio de `route.ts`, agregar a los imports existentes:

```ts
import { client } from "@/db/schema/client";
import { agency } from "@/db/schema/agency";
import { user } from "@/db/schema/better-auth";
```

- [ ] **Step 2: Fetchear splitMeta**

Después de la query que busca `overdueWithRate` y antes del loop de punitorios, agregar una query para obtener el contrato activo con datos de split:

```ts
// Obtener datos de modalidad split del contrato activo de este inquilino
const [activeContractSplit] = await db
  .select({
    paymentModality: contract.paymentModality,
    managementCommissionPct: contract.managementCommissionPct,
    ownerId: contract.ownerId,
  })
  .from(contract)
  .innerJoin(contractParticipant, eq(contractParticipant.contractId, contract.id))
  .where(
    and(
      eq(contractParticipant.clientId, id),
      eq(contractParticipant.role, "tenant"),
      inArray(contract.status, ["active", "expiring_soon"])
    )
  )
  .orderBy(contract.createdAt)
  .limit(1);
```

Nota: necesitás agregar `contractParticipant` a los imports: `import { contractParticipant } from "@/db/schema/contract-participant";`

- [ ] **Step 3: Fetchear owner CBU y agency banking (en paralelo, solo si es split)**

Después del paso anterior:

```ts
let splitMeta: {
  managementCommissionPct: number;
  ownerName: string;
  ownerCbu: string | null;
  agencyNombre: string | null;
  agenciaCbu: string | null;
  agenciaAlias: string | null;
} | null = null;

if (activeContractSplit?.paymentModality === "split") {
  const [ownerRow, agencyRow] = await Promise.all([
    db
      .select({ firstName: client.firstName, lastName: client.lastName, cbu: client.cbu })
      .from(client)
      .where(eq(client.id, activeContractSplit.ownerId))
      .limit(1)
      .then((r) => r[0] ?? null),
    db
      .select({ name: agency.name, bancoCBU: agency.bancoCBU, bancoAlias: agency.bancoAlias })
      .from(agency)
      .innerJoin(user, eq(agency.ownerId, user.id))
      .where(eq(user.id, session.user.id))
      .limit(1)
      .then((r) => r[0] ?? null),
  ]);

  splitMeta = {
    managementCommissionPct: Number(activeContractSplit.managementCommissionPct ?? 10),
    ownerName: ownerRow
      ? `${ownerRow.firstName} ${ownerRow.lastName ?? ""}`.trim()
      : "Propietario",
    ownerCbu: ownerRow?.cbu ?? null,
    agencyNombre: agencyRow?.name ?? null,
    agenciaCbu: agencyRow?.bancoCBU ?? null,
    agenciaAlias: agencyRow?.bancoAlias ?? null,
  };
}
```

- [ ] **Step 4: Asignar `beneficiario` a punitorios auto-generados en contratos split**

Dentro del loop existente que inserta punitorios (`await db.insert(tenantLedger).values({...})`), agregar el campo:

```ts
...(activeContractSplit?.paymentModality === "split" && { beneficiario: "propietario" }),
```

- [ ] **Step 5: Incluir `splitMeta` en el return**

Al final del handler, el `return NextResponse.json({...})` existente ya devuelve `kpis`, `ledgerEntries`, y `proximoAjuste`. Agregar `splitMeta` al mismo objeto sin tocar el resto:

```ts
return NextResponse.json({
  kpis: {
    estadoCuenta: hayMora ? "en_mora" : "al_dia",
    moraDetalle: hayMora ? { capital: capitalEnMora, intereses: interesesEnMora, total: capitalEnMora + interesesEnMora } : null,
    totalCobradoYTD: Number(ytdResult?.total ?? 0),
    diasPromedioPago,
    proximoPago: { total: nextMonthTotal, montoMinimo, fecha: nextMonthDueDate, tieneAjuste: nextMonthHasAdjuste },
  },
  ledgerEntries: entries,
  proximoAjuste: firstPendingRevision
    ? { period: firstPendingRevision.period, mesesRestantes: null }
    : null,
  splitMeta,   // nuevo — null cuando el contrato no es split
});
```

- [ ] **Step 6: Verificar**

`bun dev` → abrir cuenta corriente de un inquilino con contrato activo → en DevTools Network verificar que `/api/tenants/[id]/cuenta-corriente` devuelve `splitMeta: null` (o el objeto con datos si el contrato es split).

- [ ] **Step 7: Commit**

```bash
git add src/app/api/tenants/\[id\]/cuenta-corriente/route.ts
git commit -m "feat(api): return splitMeta in tenant cuenta-corriente"
```

---

## Task 4: API ledger POST — aceptar y auto-asignar `beneficiario`

**Files:**
- Modify: `src/app/api/tenants/[id]/ledger/route.ts`

- [ ] **Step 1: Agregar `beneficiario` al schema Zod**

En el `createEntrySchema` existente (línea ~13), agregar:

```ts
beneficiario: z.enum(["propietario", "administracion", "split"]).optional(),
```

- [ ] **Step 2: Auto-asignar `beneficiario` si el contrato es split**

Después de verificar el contrato (línea ~63, después de `if (!contractRow)`), agregar:

```ts
// Fetch paymentModality for auto-assignment
const [contractFull] = await db
  .select({ paymentModality: contract.paymentModality })
  .from(contract)
  .where(eq(contract.id, data.contratoId))
  .limit(1);

const isSplit = contractFull?.paymentModality === "split";

const defaultBeneficiario = isSplit
  ? data.beneficiario ??
    (data.tipo === "alquiler" ? "split" : "propietario")
  : null;
```

- [ ] **Step 3: Persistir `beneficiario` en el insert**

En el `db.insert(tenantLedger).values({...})`, agregar:

```ts
beneficiario: defaultBeneficiario ?? undefined,
```

- [ ] **Step 4: Commit**

```bash
git add src/app/api/tenants/\[id\]/ledger/route.ts
git commit -m "feat(api): auto-assign beneficiario on ledger entry creation for split contracts"
```

---

## Task 5: API conciliar — guardar `splitBreakdown`

**Files:**
- Modify: `src/app/api/tenants/[id]/ledger/[entryId]/conciliar/route.ts`

- [ ] **Step 1: Agregar `splitBreakdown` al schema Zod**

El `conciliarSchema` actual solo tiene `fecha`. Agregar:

```ts
const conciliarSchema = z.object({
  fecha: z.string().regex(/^\d{4}-\d{2}-\d{2}$/),
  splitBreakdown: z.object({
    propietario: z.number(),
    administracion: z.number(),
  }).optional(),
});
```

- [ ] **Step 2: Guardar `splitBreakdown` en el update**

En el `tx.update(tenantLedger).set({...})` (línea ~78), agregar:

```ts
...(result.data.splitBreakdown && {
  splitBreakdown: JSON.stringify(result.data.splitBreakdown),
}),
```

- [ ] **Step 3: Commit**

```bash
git add src/app/api/tenants/\[id\]/ledger/\[entryId\]/conciliar/route.ts
git commit -m "feat(api): persist splitBreakdown when conciliating ledger entry"
```

---

## Task 6: LedgerEntry tipo + LedgerTable columna Destino

**Files:**
- Modify: `src/components/tenants/ledger-table.tsx`

- [ ] **Step 1: Agregar campos al tipo `LedgerEntry`**

En la definición de `LedgerEntry` (línea ~17), agregar:

```ts
beneficiario: string | null;
splitBreakdown: string | null;
```

- [ ] **Step 2: Agregar prop `isSplitContract` y `isOwnerView` al componente**

En el type `Props` (línea ~40), agregar:

```ts
isSplitContract?: boolean;
isOwnerView?: boolean;
managementCommissionPct?: number;
```

- [ ] **Step 3: Agregar columna "Destino" al encabezado**

Dentro del `<thead>`, agregar después de la columna "TIPO" (solo cuando `isSplitContract || isOwnerView`):

```tsx
{(isSplitContract || isOwnerView) && (
  <th className="px-3 py-2 text-left text-xs font-medium text-muted-foreground">
    Destino
  </th>
)}
```

- [ ] **Step 4: Agregar celda Destino a cada fila**

Dentro de la fila de cada entrada (en el map de `entries`), agregar después de la celda Tipo:

```tsx
{(isSplitContract || isOwnerView) && (
  <td className="px-3 py-2">
    <DestinoBadge
      beneficiario={entry.beneficiario}
      managementCommissionPct={managementCommissionPct ?? 10}
      monto={Number(entry.monto ?? 0)}
      splitBreakdown={entry.splitBreakdown}
      isOwnerView={isOwnerView}
    />
  </td>
)}
```

- [ ] **Step 5: Crear el componente `DestinoBadge`**

Agregar justo antes del export de `LedgerTable`:

```tsx
function DestinoBadge({
  beneficiario,
  managementCommissionPct,
  monto,
  splitBreakdown,
  isOwnerView,
}: {
  beneficiario: string | null;
  managementCommissionPct: number;
  monto: number;
  splitBreakdown: string | null;
  isOwnerView?: boolean;
}) {
  if (isOwnerView && splitBreakdown) {
    return (
      <span className="inline-flex items-center gap-1 text-xs px-2 py-0.5 rounded-md bg-blue-950 border border-blue-800 text-blue-300 font-medium">
        ↗ Cobro directo desde inquilino
      </span>
    );
  }
  if (!beneficiario) return null;
  if (beneficiario === "split") {
    const adm = Math.round(monto * (managementCommissionPct / 100));
    const prop = monto - adm;
    return (
      <span className="flex flex-wrap gap-1">
        <span className="text-xs px-1.5 py-0.5 rounded bg-emerald-950 border border-emerald-800 text-emerald-300">
          ↗ Prop. ${prop.toLocaleString("es-AR")}
        </span>
        <span className="text-xs px-1.5 py-0.5 rounded bg-blue-950 border border-blue-800 text-blue-300">
          ↗ Adm. ${adm.toLocaleString("es-AR")}
        </span>
      </span>
    );
  }
  if (beneficiario === "propietario") {
    return (
      <span className="text-xs px-1.5 py-0.5 rounded bg-emerald-950 border border-emerald-800 text-emerald-300">
        ↗ Propietario
      </span>
    );
  }
  return (
    <span className="text-xs px-1.5 py-0.5 rounded bg-blue-950 border border-blue-800 text-blue-300">
      ↗ Administración
    </span>
  );
}
```

- [ ] **Step 6: Fila con fondo azulado para owner view + cobro directo**

En el `<tr>` de cada entrada, agregar al `className` condicional:

```tsx
className={cn(
  "...",  // clases existentes
  isOwnerView && entry.splitBreakdown ? "bg-blue-950/20" : ""
)}
```

- [ ] **Step 7: Verificar**

`bun dev` → abrir cuenta corriente de cualquier inquilino → verificar que no aparece columna Destino (contrato no es split). Verificar que no hay errores de TypeScript.

- [ ] **Step 8: Commit**

```bash
git add src/components/tenants/ledger-table.tsx
git commit -m "feat(ledger-table): add Destino column and DestinoBadge for split contracts"
```

---

## Task 7: CobroPanel — desglose split-aware

**Files:**
- Modify: `src/components/tenants/cobro-panel.tsx`

- [ ] **Step 1: Actualizar Props**

Reemplazar el type `Props` actual por:

```tsx
type SplitMeta = {
  managementCommissionPct: number;
  ownerName: string;
  ownerCbu: string | null;
  agencyNombre: string | null;
  agenciaCbu: string | null;
  agenciaAlias: string | null;
};

type Props = {
  selectedEntries: LedgerEntry[];
  montoOverrides: Record<string, string>;
  beneficiarioOverrides: Record<string, string>;
  honorariosPct: number;
  splitMeta: SplitMeta | null;
  onClearSelection: () => void;
  onEmitirRecibo: () => void;
  isEmitting: boolean;
};
```

- [ ] **Step 2: Calcular desglose por destinatario**

Agregar función de cálculo dentro del componente, después de `getMonto`:

```tsx
function computeSplitTotals(
  entries: LedgerEntry[],
  overrides: Record<string, string>,
  beneficiarioOverrides: Record<string, string>,
  pct: number
): { propietario: number; administracion: number } {
  let prop = 0;
  let adm = 0;
  for (const e of entries) {
    const monto = getMonto(e, overrides);
    const dest = beneficiarioOverrides[e.id] ?? e.beneficiario;
    if (dest === "split") {
      const admPart = Math.round(monto * (pct / 100));
      adm += admPart;
      prop += monto - admPart;
    } else if (dest === "administracion") {
      adm += monto;
    } else {
      prop += monto;
    }
  }
  return { propietario: round2(prop), administracion: round2(adm) };
}
```

- [ ] **Step 3: Reemplazar el bloque de render del desglose**

Dentro del return del componente, reemplazar el bloque "Desglose del cobro" actual por uno que sea split-aware:

```tsx
{splitMeta ? (
  // Desglose split
  <div className="flex-1 space-y-2 text-sm">
    <p className="text-xs text-muted-foreground uppercase tracking-wide mb-2">
      Desglose de transferencias
    </p>
    {(() => {
      const { propietario, administracion } = computeSplitTotals(
        selectedEntries, montoOverrides, beneficiarioOverrides,
        splitMeta.managementCommissionPct
      );
      return (
        <>
          <div className="flex items-center justify-between rounded-lg border border-emerald-800 bg-emerald-950/40 px-3 py-2">
            <div>
              <p className="font-medium text-emerald-300">{splitMeta.ownerName}</p>
              {splitMeta.ownerCbu && (
                <p className="text-xs text-muted-foreground">CBU {splitMeta.ownerCbu}</p>
              )}
            </div>
            <span className="font-mono font-bold text-emerald-300">
              ${propietario.toLocaleString("es-AR")}
            </span>
          </div>
          <div className="flex items-center justify-between rounded-lg border border-blue-800 bg-blue-950/40 px-3 py-2">
            <div>
              <p className="font-medium text-blue-300">{splitMeta.agencyNombre ?? "Administración"}</p>
              {splitMeta.agenciaCbu && (
                <p className="text-xs text-muted-foreground">CBU {splitMeta.agenciaCbu}</p>
              )}
              {splitMeta.agenciaAlias && (
                <p className="text-xs text-muted-foreground">alias: {splitMeta.agenciaAlias}</p>
              )}
            </div>
            <span className="font-mono font-bold text-blue-300">
              ${administracion.toLocaleString("es-AR")}
            </span>
          </div>
        </>
      );
    })()}
  </div>
) : (
  // Desglose original (contratos A/B)
  <div className="flex-1 space-y-1 text-sm">
    <p className="text-xs text-muted-foreground uppercase tracking-wide mb-2">
      Desglose del cobro
    </p>
    {selectedEntries.map((e) => (
      <div key={e.id} className="flex justify-between">
        <span className="text-muted-foreground truncate max-w-[200px]">
          {e.descripcion}
        </span>
        <span className="font-mono font-medium ml-4">
          ${getMonto(e, montoOverrides).toLocaleString("es-AR")}
        </span>
      </div>
    ))}
    <Separator className="my-2" />
    <div className="flex justify-between text-xs text-primary">
      <span>Honorarios ({honorariosPct}%)</span>
      <span className="font-mono">
        ${round2(selectedEntries
          .filter((e) => e.tipo !== "punitorio" && e.tipo !== "descuento")
          .reduce((s, e) => s + getMonto(e, montoOverrides), 0) * (honorariosPct / 100)
        ).toLocaleString("es-AR")}
      </span>
    </div>
    <div className="flex justify-between text-xs text-[var(--income)]">
      <span>Propietario recibe</span>
      <span className="font-mono">
        ${round2(
          selectedEntries.reduce((s, e) => s + getMonto(e, montoOverrides), 0) -
          selectedEntries
            .filter((e) => e.tipo !== "punitorio" && e.tipo !== "descuento")
            .reduce((s, e) => s + getMonto(e, montoOverrides), 0) * (honorariosPct / 100)
        ).toLocaleString("es-AR")}
      </span>
    </div>
  </div>
)}
```

- [ ] **Step 4: Commit**

```bash
git add src/components/tenants/cobro-panel.tsx
git commit -m "feat(cobro-panel): split-aware desglose with two payment destinations"
```

---

## Task 8: AddManualChargeDialog — selector de beneficiario

**Files:**
- Modify: `src/components/ledger/add-manual-charge-dialog.tsx`

- [ ] **Step 1: Agregar `isSplitContract` al tipo Props y `beneficiario` a `ManualChargeData`**

```tsx
// En ManualChargeData:
export type ManualChargeData = {
  // ... campos existentes ...
  beneficiario?: "propietario" | "administracion";
};

// En Props:
type Props = {
  open: boolean;
  onOpenChange: (open: boolean) => void;
  onSave: (data: ManualChargeData) => Promise<void>;
  isSplitContract?: boolean;
};
```

- [ ] **Step 2: Agregar estado `beneficiario`**

Dentro del componente, después de los estados existentes:

```tsx
const [beneficiario, setBeneficiario] = useState<"propietario" | "administracion">("propietario");
```

- [ ] **Step 3: Resetear en `reset()`**

En la función `reset()`, agregar:

```ts
setBeneficiario("propietario");
```

- [ ] **Step 4: Agregar selector en el formulario**

Dentro del JSX, después del campo "Período (opcional)", agregar condicionalmente:

```tsx
{isSplitContract && (
  <div className="space-y-2">
    <Label>¿A quién va este cargo?</Label>
    <Select
      value={beneficiario}
      onValueChange={(v) => setBeneficiario(v as "propietario" | "administracion")}
    >
      <SelectTrigger>
        <SelectValue />
      </SelectTrigger>
      <SelectContent>
        <SelectItem value="propietario">↗ Propietario</SelectItem>
        <SelectItem value="administracion">↗ Administración</SelectItem>
      </SelectContent>
    </Select>
  </div>
)}
```

- [ ] **Step 5: Incluir `beneficiario` al llamar `onSave`**

En la función `handleSave`, agregar `beneficiario` al objeto pasado a `onSave`:

```ts
await onSave({
  // ... campos existentes ...
  ...(isSplitContract && { beneficiario }),
});
```

- [ ] **Step 6: Commit**

```bash
git add src/components/ledger/add-manual-charge-dialog.tsx
git commit -m "feat(add-charge-dialog): add beneficiario selector for split contracts"
```

---

## Task 9: EntryDetailDialog — override efímero de beneficiario

**Files:**
- Modify: `src/components/ledger/entry-detail-dialog.tsx`

- [ ] **Step 1: Agregar `isSplitContract` a Props y `onBeneficiarioOverride` callback**

```tsx
type Props = {
  entry: LedgerEntry | null;
  onOpenChange: (open: boolean) => void;
  onSave: (data: EntryEditData) => Promise<void>;
  isSplitContract?: boolean;
  onBeneficiarioOverride?: (entryId: string, value: string | null) => void;
};
```

- [ ] **Step 2: Agregar estado `beneficiarioOverride`**

```tsx
const [beneficiarioOverride, setBeneficiarioOverride] = useState<string | null>(null);
```

Resetear en el `useEffect` cuando cambia `entry`:

```ts
setBeneficiarioOverride(null);
```

- [ ] **Step 3: Agregar selector + advertencia en el formulario**

Después de los switches de flags (impactaPropietario, etc.), agregar:

```tsx
{isSplitContract && entry && (
  <div className="space-y-2 pt-1">
    <Label className="text-sm">Destino (solo este cobro)</Label>
    <Select
      value={beneficiarioOverride ?? entry.beneficiario ?? "split"}
      onValueChange={(v) => {
        const isChanged = v !== (entry.beneficiario ?? "split");
        const newVal = isChanged ? v : null;
        setBeneficiarioOverride(newVal);
        onBeneficiarioOverride?.(entry.id, newVal);
      }}
    >
      <SelectTrigger>
        <SelectValue />
      </SelectTrigger>
      <SelectContent>
        <SelectItem value="split">Split (propietario + administración)</SelectItem>
        <SelectItem value="propietario">↗ Todo al propietario</SelectItem>
        <SelectItem value="administracion">↗ Todo a la administración</SelectItem>
      </SelectContent>
    </Select>
    {beneficiarioOverride !== null && (
      <div className="flex items-start gap-2 rounded-md border border-amber-700/50 bg-amber-950/30 px-3 py-2 text-xs text-amber-400">
        <span>⚠️</span>
        <span>
          Estás cambiando el destino original de este ítem. Esto afecta el desglose del recibo.
          El cambio aplica solo para este cobro y no queda guardado en el ítem.
        </span>
      </div>
    )}
  </div>
)}
```

- [ ] **Step 4: Commit**

```bash
git add src/components/ledger/entry-detail-dialog.tsx
git commit -m "feat(entry-detail-dialog): ephemeral beneficiario override for split contracts"
```

---

## Task 10: TenantTabCurrentAccount — conectar todo

**Files:**
- Modify: `src/components/tenants/tenant-tab-current-account.tsx`

- [ ] **Step 1: Actualizar tipo `CuentaCorrienteData`**

Agregar `splitMeta` al tipo existente:

```tsx
type SplitMeta = {
  managementCommissionPct: number;
  ownerName: string;
  ownerCbu: string | null;
  agencyNombre: string | null;
  agenciaCbu: string | null;
  agenciaAlias: string | null;
};

type CuentaCorrienteData = {
  kpis: KPIs;
  ledgerEntries: LedgerEntry[];
  proximoAjuste: { period: string | null; mesesRestantes: number | null } | null;
  splitMeta: SplitMeta | null;
};
```

- [ ] **Step 2: Agregar estado `beneficiarioOverrides`**

Dentro del componente, junto a los otros estados:

```tsx
const [beneficiarioOverrides, setBeneficiarioOverrides] = useState<Record<string, string>>({});
```

Limpiar al cambiar de tab o al desseleccionar todo:

```tsx
// En la función que limpia la selección (onClearSelection):
setBeneficiarioOverrides({});
```

- [ ] **Step 3: Implementar `handleBeneficiarioOverride`**

```tsx
function handleBeneficiarioOverride(entryId: string, value: string | null) {
  setBeneficiarioOverrides((prev) => {
    if (value === null) {
      const next = { ...prev };
      delete next[entryId];
      return next;
    }
    return { ...prev, [entryId]: value };
  });
}
```

- [ ] **Step 4: Pasar `isSplitContract` y `managementCommissionPct` a `LedgerTable`**

```tsx
<LedgerTable
  // ... props existentes ...
  isSplitContract={data.splitMeta !== null}
  managementCommissionPct={data.splitMeta?.managementCommissionPct ?? 10}
/>
```

- [ ] **Step 5: Pasar `splitMeta` y `beneficiarioOverrides` a `CobroPanel`**

```tsx
<CobroPanel
  selectedEntries={selectedEntries}
  montoOverrides={montoOverrides}
  beneficiarioOverrides={beneficiarioOverrides}
  honorariosPct={data.splitMeta?.managementCommissionPct ?? honorariosPct}
  splitMeta={data.splitMeta ?? null}
  onClearSelection={...}
  onEmitirRecibo={...}
  isEmitting={...}
/>
```

- [ ] **Step 6: Pasar `isSplitContract` al `AddManualChargeDialog`**

```tsx
<AddManualChargeDialog
  open={showManual}
  onOpenChange={setShowManual}
  onSave={handleManualCharge}
  isSplitContract={data.splitMeta !== null}
/>
```

- [ ] **Step 7: Pasar `isSplitContract` y `onBeneficiarioOverride` al `EntryDetailDialog`**

```tsx
<EntryDetailDialog
  entry={selectedDetailEntry}
  onOpenChange={...}
  onSave={...}
  isSplitContract={data.splitMeta !== null}
  onBeneficiarioOverride={handleBeneficiarioOverride}
/>
```

- [ ] **Step 8: Incluir `splitBreakdown` al conciliar**

Buscar la `emitirMutation` (o donde se llama el endpoint de conciliar). Al hacer el `fetch` a `/api/tenants/${inquilinoId}/ledger/${id}/conciliar`, calcular y enviar `splitBreakdown`:

```tsx
// Función helper para calcular splitBreakdown de una entrada
function calcSplitBreakdown(
  entry: LedgerEntry,
  monto: number,
  beneficiarioOverrides: Record<string, string>,
  splitMeta: SplitMeta | null
): { propietario: number; administracion: number } | undefined {
  if (!splitMeta) return undefined;
  const dest = beneficiarioOverrides[entry.id] ?? entry.beneficiario ?? "split";
  if (dest === "split") {
    const adm = Math.round(monto * (splitMeta.managementCommissionPct / 100));
    return { propietario: monto - adm, administracion: adm };
  }
  if (dest === "administracion") return { propietario: 0, administracion: monto };
  return { propietario: monto, administracion: 0 };
}

// En el fetch de conciliar (dentro de la mutación de emitir):
const splitBreakdown = calcSplitBreakdown(entry, monto, beneficiarioOverrides, data.splitMeta);

await fetch(`/api/tenants/${inquilinoId}/ledger/${entryId}/conciliar`, {
  method: "POST",
  headers: { "Content-Type": "application/json" },
  body: JSON.stringify({
    fecha: today,
    ...(splitBreakdown && { splitBreakdown }),
  }),
});
```

- [ ] **Step 9: Verificar en UI**

`bun dev` → crear/usar un contrato con modalidad "split" → abrir cuenta corriente del inquilino → verificar:
- Columna "Destino" aparece
- CobroPanel muestra dos filas (propietario + administración) al seleccionar ítems
- AddManualChargeDialog muestra selector "¿A quién va este cargo?"
- EntryDetailDialog muestra selector de destino con advertencia al cambiar

- [ ] **Step 10: Commit**

```bash
git add src/components/tenants/tenant-tab-current-account.tsx
git commit -m "feat(tenant-account): wire split payment — beneficiario overrides, splitMeta, and breakdown"
```

---

## Task 11: OwnerTabCurrentAccount — visual distinción cobro directo

**Files:**
- Modify: `src/components/owners/owner-tab-current-account.tsx`

- [ ] **Step 1: Pasar `isOwnerView` a `LedgerTable`**

En `OwnerTabCurrentAccount`, donde se renderiza `<LedgerTable>`, agregar:

```tsx
<LedgerTable
  entries={ledgerEntries}
  montoOverrides={{}}
  selectedIds={new Set()}
  onToggleSelect={() => {}}
  onSelectMonth={() => {}}
  onDeselectMonth={() => {}}
  onMontoChange={() => {}}
  onAnularRecibo={() => {}}
  onCancelEntry={() => {}}
  onViewDetail={() => {}}
  activeFilters={new Set()}
  isOwnerView={true}
/>
```

- [ ] **Step 2: Asegurarse de que la API de owner devuelve `splitBreakdown`**

Verificar `src/app/api/owners/[id]/cuenta-corriente/route.ts` — el SELECT ya usa `getTableColumns(tenantLedger)` o similar. Si no, agregar `splitBreakdown` a los campos seleccionados.

Abrir `src/app/api/owners/[id]/cuenta-corriente/route.ts` y verificar que el select incluye `tenantLedger.splitBreakdown`. Si usa columnas explícitas, agregar:

```ts
splitBreakdown: tenantLedger.splitBreakdown,
```

- [ ] **Step 3: Verificar en UI**

`bun dev` → abrir cuenta corriente de un propietario que tenga entradas de contratos split → verificar:
- Entradas con `splitBreakdown` tienen fondo azulado y badge "↗ Cobro directo desde inquilino"
- Entradas sin `splitBreakdown` se ven iguales que antes

- [ ] **Step 4: Commit**

```bash
git add src/components/owners/owner-tab-current-account.tsx src/app/api/owners/\[id\]/cuenta-corriente/route.ts
git commit -m "feat(owner-account): highlight direct-payment entries from split contracts"
```

---

## Task 12: Actualizar PENDIENTES.md

- [ ] **Step 1: Mover "Configurar honorarios por contrato" de Prioridad alta a Completados**

En `PENDIENTES.md`, eliminar el ítem de Prioridad alta y agregar a la sección completados:

```markdown
- [x] **Modalidad de pago dividido (honorarios por contrato)** — `paymentModality = "split"`, columna `beneficiario` en ledger, desglose en CobroPanel, cobro directo en cuenta del propietario
```

- [ ] **Step 2: Agregar entrada a HISTORIAL.md**

```markdown
## 2026-05-05 — Modalidad de pago dividido

Implementa la modalidad "split" en contratos: el inquilino transfiere directamente una parte al propietario y otra a la administración. Cada entrada del ledger lleva su `beneficiario`; el CobroPanel muestra el desglose con CBU de cada destinatario; la cuenta del propietario distingue visualmente los cobros directos.

Archivos clave: `tenant-ledger.ts`, `cobro-panel.tsx`, `tenant-tab-current-account.tsx`, `entry-detail-dialog.tsx`, `add-manual-charge-dialog.tsx`
```

- [ ] **Step 3: Commit**

```bash
git add PENDIENTES.md HISTORIAL.md
git commit -m "docs: mark split payment modality as complete"
```
